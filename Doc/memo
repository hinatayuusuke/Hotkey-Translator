前処理にグレースケールを追加
#Paddle OCRは自信スコアを送るので、Paddle OCR使用している時だけ自信スコアの閾値を設定可能にして、閾値以下の翻訳結果を破棄する
履歴機能追加、翻訳結果を指定件数でホットキーで表示を切り替えられる。
全体オーバレイ表示でまとめて表示、スクロール可能にする
WinRTは選択言語に応じて、OCR認識パッケージの取得を自動化する
WPF (C#): OCR（画面認識）、翻訳API連携、ユーザー設定、DLL注入の実行。

OBSのGithubコードを参考に、DLL (C++ + ImGui): ゲームプロセス内でのDirectX、Vulkenフック、共有メモリの監視、テキスト描画。

通信: 共有メモリ（Memory Mapped File）による高速データ転送。
# F11は原文、翻訳切り替え
# F9は非表示と最新結果の表示切り替え
# フォーカスウィンドウ固定ホットキー
# ROI選択ホットキー
複数ROI登録開始ホットキー、登録完了ホットキー、新しいROI選択で登録リセット
# キャプチャウィンドウ右下にぐるぐる表示
# 中国語繁體文字のIDコード
# LlamaCppの優先度順反映
# HY-MT1.5-1.8B-Q8_0.ggufをデフォルトモデルとする
初回起動時にSetupでダイアログ表示、ダウンロードしたいモデルを選択
ローカル翻訳有効UIをメインに移行
セッティングのスクロールは左固定
初期化のウィンドウを幅広く、またはUI配置調整
ログはクリアして1から
ログなしならログウィンドウを隠す
OCR画面プレビューはメインに移動
枠拡大スライダー、一定サイズ以下の枠を倍率拡大

＃ 自動翻訳機能は画面変動でOCR、連打防止、OCR結果が前回との変化量が小さいならオーバーレイを更新しない、UI以外の認識文字数がめちゃ少ないならUI以外のオーバーレイ非表示
数回取得することでUIだと判定

F10系は SkipTranslationCache=true で動きます。
このとき同じ文が複数あっても、翻訳APIには1回しか送らない（重複排除）。
返ってきた翻訳は「送った代表1件」にだけ入ります。
本来なら同じ文の他の枠にも配るべきですが、PipelineOrchestrator.cs (line 514) の早期 return でそこで終了します。
その結果、同じ英文が画面内に2つあると、片方だけ翻訳・もう片方は英語のままが起きます。
つまりこの行は「キャッシュを使わない実行時に、重複への再配布処理をスキップしてしまう」ポイントです。